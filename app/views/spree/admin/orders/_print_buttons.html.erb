<% if @order.completed_at? %>
  <%= button_link_to('Invoice', spree.admin_order_path(@order, :pdf , :template => :invoice)) %>
  <%= button_link_to('Packaging Slip', spree.admin_order_path(@order, :pdf , :template => :packaging_slip)) %>
<% end %>
